PGDATABASE=nzbidx
PGUSER=nzbidx
PGPASSWORD=nzbidx
# Default to an uncommon host port to avoid conflicts during tests
POSTGRES_PORT=15432
# DATABASE_URL=postgres://${PGUSER}:${PGPASSWORD}@postgres:5432/${PGDATABASE}

# Core services
OPENSEARCH_URL=http://opensearch:9200
REDIS_URL=redis://redis:6379/0

# API controls
API_KEYS=dev
SAFESEARCH=on
ALLOW_XXX=false
RATE_LIMIT=60
RATE_WINDOW=60

# NNTP ingest (set values to enable)
NNTP_HOST=
NNTP_PORT=119
NNTP_USER=
NNTP_PASS=
NNTP_GROUPS=
